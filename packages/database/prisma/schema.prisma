// TML Prisma Schema
// This is the database schema for the Transparency Middleware Layer.
// Run `npx prisma migrate dev` to apply changes.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Schema models will be added in subsequent prompts.
// This file establishes the Prisma configuration.
